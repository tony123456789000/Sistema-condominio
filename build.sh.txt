#!/usr/bin/env bash
# exit on error
set -o errexit

pip install -r requirements.txt

# Usamos 'python -m flask' que es una forma más robusta de llamar a los comandos
python -m flask db init
python -m flask db migrate -m "Initial migration"
python -m flask db upgrade
python -m flask init-db # Ejecutamos nuestro nuevo comando para crear la BD y los usuarios
